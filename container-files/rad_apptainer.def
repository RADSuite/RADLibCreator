BootStrap: docker
From: ubuntu:22.04
From: nextflow/nextflow
From: condaforge/mambaforge

%files
rad_nextflow_conda.yml

%post
   apt-get -y update
   apt-get -y install unzip
   conda env create -f rad_nextflow_conda.yml && conda clean -afy

%environment
   export PATH="/opt/conda/envs/RAD_nextflow_conda/bin:${PATH}"

%labels
   Author Remington Manning-Neal 
